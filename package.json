{
  "name": "monclubhouse",
  "version": "1.0.0",
  "description": "MonClubHouse - Gestion de club sportif (workspace root)",
  "private": true,
  "scripts": {
    "postinstall": "npm install --prefix back",
    "build": "npm install --prefix back && npm install --prefix front --production=false && npm run build --prefix front",
    "start": "node back/server.js",
    "dev": "npm run dev --prefix back",
    "migrate": "npm run migrate --prefix back",
    "migrate:undo": "npm run migrate:undo --prefix back"
  },
  "dependencies": {
    "qrcode-terminal": "^0.12.0",
    "stripe": "^22.3.0",
    "whatsapp-web.js": "^1.34.7"
  }
}
